// * Cualquier código js se almacena en el Global Object Window.

// * JS es case sensitive, las mayúsculsa y minúsculas importan.

 // *JS solo corre un proceso a la vez.

 // *JS es síncrono, no puede ejecutar tareas de manera simultánea.

 // * JS permite escritura dinámica, es decir: una varialbe string se le puede asignar cualquier otro tipo de dato.

 // * Todo aquel tipo de dato que no sea primitivo (number, string, boolean, null, undefined) será considerado un objeto.

 
 
 // Algunos formas para imprimir en consola son:
console.log();
console.warn();
console.error();
console.info();

// Snipper JS:
// console.log( b ); // Uncaught ReferenceError: Cannot access 'b' before initializazion
let b = 10;

 // Comparando undefined
console.log(undefined === null)  //* false
console.log(undefined == null); //* true






